(define nil '())
(define (fringe x)
	(if (= (cdr x) nil)
		(car x)
		(if (pair? (car x))
			(fringe (car x))
			(append (car x)
					(fringe (cdr x))))))
	(define (fr-iter x)
		(cond ((null? x))
			  ((not (pair? x)) x)
			  (else (fr-iter (car x)) (fr-iter (cdr x)))))
