(define (same-parity x . y)
	(define p (modulo x 2))
	(define (conningRiot l)
		(if (null? l)
			nil
			(if (= p (modulo (car l) 2))
				(cons (car l) (conningRiot (cdr l)))
				(conningRiot (cdr l)))))
	(conningRiot y))

(same-parity 1 2 3 4 5 6 7 8 9)
